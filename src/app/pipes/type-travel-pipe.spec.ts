import { TypeTravelPipe } from './type-travel-pipe';

describe('TypeTravelPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeTravelPipe();
    expect(pipe).toBeTruthy();
  });
});
